const ValidateStatus = Object.freeze({
    VALID: 'VALID',
    INVALIDCHARS: 'INVALIDCHARS',
    INVALID81: 'INVALID81'
})

const SolverStatus = Object.freeze({
    VALID: 'VALID',
    INVALID: 'INVALID'
})

module.exports = {
    ValidateStatus,
    SolverStatus
}